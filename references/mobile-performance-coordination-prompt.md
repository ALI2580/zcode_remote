# 发给当前性能/结构优化任务的协作补充

请继续正在执行的性能与结构专项。另一个任务将并行优化手机竖屏体验，执行文件为 `references/mobile-portrait-optimization-execution-prompt.md`；请采用以下增量协作规则，不重启已完成批次。

1. 在你现有清单记录当前拥有的文件、准备拆出的模块/API、稳定批次源码身份和下一批计划。移动端在独立 worktree 开发，默认维护 `lib/ui/mobile/`、`test/ui/mobile/`、`references/mobile/`。
2. 你继续拥有协议/索引/缓存/通知/生命周期与架构拆分；手机任务拥有导航呈现、触控尺寸、弹层和竖屏布局。shell/settings/chat/composer/review/terminal 是共享入口，修改后提供稳定接口；不要同时向同一 checkout 覆盖这些文件。
3. 设置中心正在拆分时，先完成当前低耦合批次，将新页面参数、controller owner、dispose 和 scope 契约写明；手机任务接入新边界，不把旧巨型页面重新复制回来。后续聊天/外壳拆分同理。
4. 每个可集成批次可读取 `references/mobile/integration-request.md`（尚未存在则继续独立工作）。按 base/当前源码/移动 patch 三方整合，不整文件替换，不撤销彼此已有改动。集成前后核对测试与变更归属。
5. 手机允许有意改变交互容器与密度；保留官方业务/门控、主题与宽屏基线。不要用桌面像素同态要求回退已经确认的手机适配；手机差异独立记账。
6. 同一目录 Flutter/Gradle 串行，设备与 QA 包占用另行协调；不杀他方构建/应用，不覆盖证据。发布/真实远端写授权仍按既有范围。
7. 性能原基准保持 fixture/模式/设备口径；手机场景单列。集成后复测受影响热路径和作用域回归，不能把更少内容/不同窗口条件解释成算法收益，也不能让适配重新引入全量重建或重复订阅。
8. 两个任务各自维护清单，独立工作区通过不等于集成通过。你无需等待整个移动专项完成才继续独立性能批次；最终涉及移动变更的联合结论必须有同源码集成验证。

请保留当前进度，从当前批次继续，并在下一个稳定点记录可供移动端接入的接口与源码身份。
